@model IEnumerable<gestionmateriales.Models.GestionMateriales.pedido>
@{
    ViewBag.Title = "Pedidos";
}

<div id="page-wrapper">
    <div class="row">
        <div class="container">
            <h2>Buscar Pedido</h2>
        </div>
    </div>
    <hr />
    <div class=" row">

        @using (Html.BeginForm("Buscar", "Pedidos", FormMethod.Get))
        {
            <div class="col-xs-offset-6 col-md-3">
                @Html.TextBox("SearchString", ViewBag.CurrentFilter as string, new { @class = "form-control", placeholder = "Buscar...", maxlength = 45 })
            </div>
            <button class="btn btn-default" type="submit">Buscar</button>
        }

    </div>

    <br>
    <div class="row">
        <div class="col-xs-offset-2 col-md-8">
            <table class="table table-bordered table-striped table-hover text-center table-condensed">
                <tr>
                    <th>
                        @Html.ActionLink("nroPedido", "Buscar", new { sortOrder = ViewBag.NroPedidoSortParm, currentFilter = ViewBag.CurrentFilter })
                    </th>
                    <th>
                        @Html.ActionLink("nroOrdenDeTrabajo", "Buscar", new { sortOrder = ViewBag.nroOrdenDeTrabajoSortParm, currentFilter = ViewBag.CurrentFilter })
                    </th>
                    <th>
                        Destino
                    </th>
                    <th>
                        Editar
                    </th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.nroPedido)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.nroOrdenDeTrabajo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.destino)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "Editar", new { id = item.nroPedido })
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>
